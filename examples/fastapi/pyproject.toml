[project]
name = "fastapi-demo"
version = "0.1.0"
description = "FastAPI + Oban example application"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
    "oban",
]

[tool.uv.sources]
oban = { path = "../..", editable = true }
